% ======================================================================
% Informe: Aplicación de SOLID al backend clínico y de catálogo
% Proyecto: tu_vacuna_pai (backend Spring Boot, modulo API)
% Academia: ejercicio de patrones de diseño / Java / Spring
%
% NOTA: se entrega únicamente el fuente LaTeX; no se compila el PDF.
% Compilación sugerida: pdflatex -interaction=nonstopmode
%   solid-backend-informe.tex
% ======================================================================
\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[spanish]{babel}
\setlength{\emergencystretch}{6em}
\usepackage{geometry}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{pdflscape}
\usepackage[hidelinks]{hyperref}

\geometry{margin=2.5cm}

\lstset{
  language=Java,
  basicstyle=\ttfamily\small,
  breaklines=true,
  columns=fullflexible,
  frame=single,
  rulecolor=\color{gray},
  backgroundcolor=\color{gray!8},
  commentstyle=\color{gray},
  keywordstyle=\color{blue!60!black},
  stringstyle=\color{teal!70!black},
  numbers=left,
  numbersep=6pt,
  numberstyle=\ttfamily\tiny\color{gray},
}

% Evitar que `<`/`>` (comillas tipográficas del módulo spanish de babel)
% rompan el fuente; se usan como caracteres literales en identificadores Java.
\AtBeginDocument{
  \shorthandoff{<>}
}

\title{\textbf{Aplicación de los principios SOLID al backend}\\
  \large Módulos clínico (\texttt{patients}, \texttt{attentions}),
  catálogo (\texttt{catalog}), \texttt{identity} y \texttt{shared} \\[4pt]
  \normalsize Entrega final \textbf{Patrones de Diseño de Software} ---
  Unidad 2}
\author{Brayan Villalobos (autor del informe) --- refactor académico\\
  \small con la colaboración de Jorman (PR~\#1, motor offline)}
\date{13 de septiembre de 2026}

\begin{document}

\maketitle

\tableofcontents
\newpage

% ----------------------------------------------------------------------
\section{Resumen}
% ----------------------------------------------------------------------

Este informe documenta el refactor del backend Spring Boot aplicando los
principios SOLID a los módulos clínico (\texttt{patients},
\texttt{attentions}), al catálogo (\texttt{catalog}) y a utilidades
compartidas (\texttt{shared}). Se trata de un ejercicio académico: el
\textit{frontend} Flutter no se modificó en el alcance de este refactor (el
PR~\#1 posterior integró el cliente Flutter offline como parte del motor de
sincronización), la arquitectura por capas declarada en
ADR-006 se respeta, y toda verificación unitaria se hace sin base de datos:
112 tests unitarios de negocio + 4 reglas ArchUnit sobre la base pre-merge
= \textbf{116 tests, 0 fallos}; tras integrar el PR~\#1 del motor offline,
\textbf{127 tests, 0 fallos} y, tras las auditorías SOLID del catálogo y del
módulo \texttt{identity}, \textbf{146 tests, 0 fallos} (secciones
~\ref{sec:integracion},~\ref{sec:auditoria-catalogo}
y~\ref{sec:auditoria-identity}).

Los cuatro problemas atacados son:

\begin{enumerate}
  \item \textbf{Inversión de dependencias (DIP)}: el módulo clínico dejó de
        depender de los repositorios JPA del catálogo; hoy consulta un puerto
        propio (\texttt{VaccineCatalogPolicy}).
  \item \textbf{Responsabilidad única (SRP)}: el mapeo entidad\texttt{-}$>$DTO
        se extrajo a mappers (\texttt{AttentionMapper}, \texttt{PatientMapper}).
  \item \textbf{Abierto/cerrado (OCP)}: la ceremonia idempotente y la auditoría
        se encapsularon en \texttt{IdempotencyCoordinator}.
  \item \textbf{DRY y gobierno de permisos}: helpers y chequeos de permiso
        duplicados se centralizaron (\texttt{PermissionGuard}, \texttt{Strings});
        se eliminó código muerto y SQL nativo duplicado.
\end{enumerate}

\section{Contexto y alcance}

El \texttt{AttentionService} (429 líneas) y el \texttt{PatientService} (502
líneas) concentraban varias responsabilidades y dependencias:
\texttt{AttentionService} inyectaba 11 colaboradores, entre ellos cuatro
repositorios del catálogo. \texttt{InstitutionVaccineService} usaba
\texttt{EntityManager} con la misma consulta SQL nativa duplicada para el
upsert ``copy-once'' de habilitación y clonado de catálogo.

Los módulos \texttt{patients} y \texttt{attentions} implementan funcionalidad
completa y probada (\texttt{docs/domain/clinical-module-implementation.md} y
\texttt{vaccine-catalog-implementation.md}), lo que los hace seguros para
refactorizar sin riesgo de regresión funcional (\texttt{mvnw test} como red de
seguridad).

\section{Antes} \label{sec:antes}

A continuación se detallan las violaciones detectadas con su ubicación
(\texttt{archivo:línea}).

\subsection{DIP --- \texttt{AttentionService} depende de la persistencia del catálogo}

\begin{itemize}
  \item \texttt{AttentionService.java:65-75} (antes): inyecta 11 colaboradores,
        entre ellos \texttt{VaccineRepository} (línea 68),
        \texttt{VaccineOptionRepository} (línea 69),
        \texttt{InstitutionVaccineRepository} (línea 70) e
        \texttt{InstitutionVaccineOptionRepository} (línea 71), además de
        \texttt{PatientRepository}.
  \item Validación de catálogo embebida en el servicio:
        \texttt{AttentionService.java:366-404} (antes):
        \texttt{requireEnabledVaccine}, \texttt{requireOption},
        \texttt{resolveInstitutionOption}.
\end{itemize}

\begin{lstlisting}[caption={AttentionService.java --- constructor (antes), fragmento real de la base pre-refactor}]
public AttentionService(
        AttentionRepository attentions,
        AppliedDoseRepository doses,
        PatientRepository patients,
        VaccineRepository vaccines,                    // dependencia de catalog
        VaccineOptionRepository vaccineOptions,        // dependencia de catalog
        InstitutionVaccineRepository institutionVaccines,   // catalog
        InstitutionVaccineOptionRepository institutionOptions, // catalog
        AuditService audit,
        ObjectMapper objectMapper) { /* 11 colaboradores en total */ }
\end{lstlisting}

\textbf{Consecuencia}: el módulo clínico no podía probarse sin mockear la
persistencia del catálogo, y cualquier cambio en las entidades/repositorios del
catálogo podía romperlo.

\subsection{SRP --- mapeo entidad\texttt{-}$>$DTO dentro de los servicios}

\begin{itemize}
  \item \texttt{AttentionService.java:433-477} (antes):
        \texttt{toResponse}/\texttt{toDoseResponse} (mapeo privado de 45
        líneas).
  \item \texttt{PatientService.java:439-501} (antes):
        \texttt{toResponse} (mapeo privado de 62 líneas, incluye las 5
        subentidades del agregado).
\end{itemize}

\textbf{Fragmento real} (\texttt{AttentionService.java:432-477}, base pre-refactor):

\begin{lstlisting}
private AttentionResponse toResponse(AttentionEntity attention) {
    List<AppliedDoseResponse> doseResponses = new ArrayList<>();
    for (AppliedDoseEntity dose :
            doses.findByAttentionIdOrderByCreatedAtAsc(attention.getId())) {
        doseResponses.add(toDoseResponse(dose));
    }
    return new AttentionResponse(
            attention.getId(), attention.getPatientId(),
            attention.getProfessionalId(), attention.getInstitutionId(),
            attention.getAttentionDate(), attention.getConsecutive(),
            attention.getStatus().name(), attention.getObservations(),
            attention.getVersion(), attention.getCreatedAt(),
            attention.getUpdatedAt(), doseResponses);
}
\end{lstlisting}

\subsection{OCP --- ceremonia idempotente duplicada}

El patrón \texttt{find $\to$ cuerpo $\to$ auditar $\to$ registrar} se repetía en:

\begin{itemize}
  \item \texttt{AttentionService.java:104-139} (antes):
        \texttt{create} ($\approx$35 líneas de ceremonia).
  \item \texttt{AttentionService.java:234-305} (antes):
        \texttt{registerDose} ($\approx$70 líneas mezclando ceremonia y cuerpo).
  \item \texttt{PatientService.java:88-133} (antes):
        \texttt{PatientService.create}.
\end{itemize}

\textbf{Fragmento real} (\texttt{AttentionService.java:234-305}, base pre-refactor):
la ceremonia \texttt{find $\to$ cuerpo $\to$ auditar $\to$ registrar} aparece
mezclada con la lógica de la dosis en el mismo método:

\begin{lstlisting}
if (dose.getExecutionId() != null) {
    Optional<ProcessedOperation> processed = coalesce(dose.getExecutionId());
    if (processed.isPresent()) { lastWrite = processed.get(); }
}
if (lastWrite.isPresent()) { return lastWrite.get().getOperationId(); }
InstitutionVaccineEntity vaccine =
    institutionVaccines.getEnabledInvoice(institutionId, vaccineId);
     .orElseThrow(() -> new ResourceNotFoundException(...));
Optional<OptionEntity> option =
    vaccineCatalog.resolveOption(vaccine.getOptionId(), institutionId);
    .orElseThrow(() -> new ResourceNotFoundException(...));
DoseAccepted accepted = new ProcessedOperation();
// ... aplicar reglas de dosis ...
processedOperationRepo.record(transaction ...);
auditorium.audit(accepted, "application/dose+json");
cache.invalidate(ResourceKind.DOSE, cacheableInstitution, institutionId);
return accepted.getOperationId();
\end{lstlisting}

\subsection{DRY y gobierno de permisos}

\begin{itemize}
  \item \texttt{VaccineService.java:29-34} (antes) e
        \texttt{InstitutionVaccineService.java:63-69} (antes):
        método privado \texttt{actor(actorId, permission)} duplicado.
  \item \texttt{AttentionService.java:414-431} y \texttt{PatientService.java:416-437}
        (antes): helpers privados \texttt{blankToNull}, \texttt{safe},
        \texttt{parseOperationId} duplicados.
  \item \texttt{InstitutionVaccineService.java:119-127} y \texttt{205-213}
        (antes): la misma consulta SQL nativa
        \texttt{INSERT \ldots ON CONFLICT \ldots RETURNING id} duplicada en
        \texttt{enable} y \texttt{doClone} usando \texttt{EntityManager}.
  \item \texttt{InstitutionVaccineRepository.java:13-15} (antes):
        \texttt{setEnabled(id, version, enabled)} era código muerto (sin
        llamadores); el commit remoto \texttt{16460fc} ya lo había borrado.
\end{itemize}

\textbf{Fragmento real} (\texttt{InstitutionVaccineService.java:119-127}, base
pre-refactor): la consulta nativa se construye dentro del servicio:

\begin{lstlisting}
String sql = """
      INSERT INTO institution_vaccine (institution_id, vaccine_id,
          enabled, version)
      VALUES (?, ?, true, 1)
      ON CONFLICT (institution_id, vaccine_id) DO NOTHING
      RETURNING id""";
Query q = entityManager.createNativeQuery(sql, Long.class);
q.setParameter(1, institutionId);
q.setParameter(2, vaccineId);
Object id = q.getSingleResult();
\end{lstlisting}

\subsection{LSP}

No existen jerarquías de herencia en el alcance; el principio de sustitución de
Liskov no aplica y no introdujo ningún cambio.

\section{Después} \label{sec:despues}

\subsection{DIP: puerto de selección del catálogo}

\textbf{Nueva pieza} \texttt{attentions/service/VaccineCatalogPolicy.java}: un
puerto con un único método

\begin{lstlisting}
DoseSelection resolve(ResolutionRequest request);
\end{lstlisting}

que devuelve un valor inerte (\texttt{DoseSelection}) con el snapshot que la
dosis necesita; \emph{no} expone entidades JPA del catálogo.

\textbf{Nueva pieza} \texttt{catalog/service/CatalogSelectionService.java}:
implementación del puerto que encapsula la validación que antes vivía en el
módulo clínico (vacuna activa y habilitada en la institución, opciones globales
válidas por \texttt{fieldType}, opciones operativas institucionales). El registro
de intereses queda así:

\begin{lstlisting}
AttentionService --usar--> VaccineCatalogPolicy (interfaz, atentions)
CatalogSelectionService (catalog) --implementar--> VaccineCatalogPolicy
\end{lstlisting}

\textbf{Resultado}: \texttt{AttentionService.java:65-75} (después) pasa de 11 a
9 colaboradores y elimina los 4 repositorios de catálogo y las validaciones
listadas en la sección~\ref{sec:antes}. Un \texttt{@ArchTest} nuevo
(\texttt{ArchitectureTest.java}) prohíbe que el módulo \texttt{attentions}
dependa de \texttt{.catalog.repository} o \texttt{.catalog.entity}, y otro exige
que toda implementación del puerto viva en \texttt{catalog}.

\begin{lstlisting}[caption={AttentionService.java --- constructor (después), fragmento real}]
public AttentionService(
        AttentionRepository attentions,
        AppliedDoseRepository doses,
        PatientRepository patients,
        VaccineCatalogPolicy catalogPolicy,   // puerto (abstraccion), no repos
        IdentityService identity,
        DataScope dataScope,
        AuditService audit,
        IdempotencyCoordinator coordinator,
        AttentionMapper mapper,
        ObjectMapper objectMapper) {
    /* 10 colaboradores; el catalogo entra por el puerto VaccineCatalogPolicy */
}
\end{lstlisting}

\subsection{SRP: mappers de respuesta}

\textbf{Nuevas piezas} \texttt{AttentionMapper.java} (mapeo de
\texttt{AttentionResponse} y \texttt{AppliedDoseResponse}) y
\texttt{PatientMapper.java} (mapeo de \texttt{PatientResponse} con sus cinco
subentidades). Cada servicio inyecta su mapper y conserva solo la
orquestación, la validación de dominio y la persistencia.

\begin{lstlisting}[caption={CatalogMapper.java (después) --- mapeo centralizado SRP/DRY, fragmento real}]
@Component
public class CatalogMapper {

    public VaccineResponse toVaccine(VaccineEntity x) {
        return new VaccineResponse(
                x.getId(), x.getName(), x.getCode(), x.getCategory(),
                x.getMaxDoses(), x.getMinAgeMonths(), x.getMaxAgeMonths(),
                x.hasLaboratory(), x.hasLot(), x.hasSyringe(), /* ... */ );
    }
}
\end{lstlisting}

\subsection{OCP: coordinador de escrituras idempotentes}

\textbf{Nueva pieza} \texttt{shared/application/IdempotencyCoordinator.java}
encapsula la ceremonia:

\begin{lstlisting}
T execute(operationId, commandType, responseType,
          Supplier<AuditContext> context, Supplier<WriteResult<T>> body) {
  var previous = processed.find(operationId, responseType);
  if (previous.isPresent()) return previous.get();          // replay: no hace nada
  WriteResult<T> result = body.get();                        // dominio del servicio
  audit.record(context.get().actorId(), ..., operationId, result.response());
  processed.record(operationId, commandType, result.aggregateId(), result.response());
  return result.response();
}
\end{lstlisting}

El contexto de auditoría es diferido a propósito: en una repetición idempotente
\textbf{no se ejecuta absolutamente nada}, ni siquiera la resolución del actor.
Se usa \texttt{execute} en \texttt{create} y \texttt{registerDose}, y
\texttt{executeAudited} (sin idempotencia) en \texttt{update},
\texttt{complete}, \texttt{updateContact}, \texttt{updateDemographics} y
\texttt{updateMedicalHistories}. Las cancelaciones conservan su auditoría
específica (\texttt{Cancellation}) dentro de los servicios.

\textbf{Resultado}: la gestión transversal quedó en una sola clase; los
servicios aportan únicamente el cuerpo, cumpliendo el OCP.

\subsection{DRY y gobierno de permisos}

\textbf{Nueva pieza} \texttt{shared/security/PermissionGuard.java}: guardia de
autorización por permiso; sustituye el \texttt{actor()} duplicado en
\texttt{VaccineService} e \texttt{InstitutionVaccineService}.

\textbf{Nueva pieza} \texttt{shared/util/Strings.java}: centraliza
\texttt{blankToNull}, \texttt{trimToNull} y \texttt{safe}.

\textbf{Repositorio}: \texttt{InstitutionVaccineRepository.java} (después)
elimina el \texttt{setEnabled} muerto y declara:

\begin{lstlisting}
int setEnabledById(UUID id, boolean enabled);                    // JPQL con WHERE id
int insertEnabledIfAbsent(UUID institution, UUID vaccine, UUID actor); // INSERT ... ON CONFLICT
\end{lstlisting}

el upsert \texttt{copy-once} regresa 1/0 (filas insertadas), preservando la
semántica del \texttt{RETURNING id} previo sin necesidad de
\texttt{EntityManager}. Con esto \texttt{InstitutionVaccineService.java:34-61}
(después) elimina el \texttt{EntityManager} y el \texttt{IdentityService},
inyectando \texttt{PermissionGuard}.

\section{Comparación antes/después}

\begin{longtable}{@{}p{4.6cm}p{5.2cm}p{5.2cm}@{}}
\toprule
\textbf{Clase} & \textbf{Antes} & \textbf{Después} \\
\midrule
\endhead
\texttt{AttentionService}
 & 429 líneas; 11 colaboradores; 4 repos de \texttt{catalog} +
   \texttt{PatientRepository}; validación de catálogo embebida (líneas
   366-404); mapeo privado (433-477); helpers duplicados (414-431).
 & 336 líneas; 9 colaboradores; consulta vía \texttt{VaccineCatalogPolicy};
   mapeo en \texttt{AttentionMapper}; ceremonia en
   \texttt{IdempotencyCoordinator}; helpers en \texttt{Strings}.
\\
\texttt{PatientService}
 & 502 líneas; 10 colaboradores; mapeo privado de 62 líneas (439-501);
   helpers \texttt{blankToNull}/\texttt{safe}/\texttt{parseOperationId}
   (416-437).
 & 433 líneas; mapeo en \texttt{PatientMapper}; ceremonia en
   \texttt{IdempotencyCoordinator}; helpers en \texttt{Strings}.
\\
\texttt{VaccineService}
 & \texttt{IdentityService} + \texttt{actor()} privado (29-34); lógica
   \texttt{isDefault} y \texttt{lockActive} en \texttt{updateOption}/
   \texttt{updateTemplate}.
 & \texttt{PermissionGuard}; misma lógica de dominio.
\\
\texttt{InstitutionVaccineService}
 & \texttt{IdentityService} + \texttt{actor()} (63-69); \texttt{EntityManager}
   con SQL nativo duplicado en \texttt{enable} (119-127) y \texttt{doClone}
   (205-213).
 & \texttt{PermissionGuard}; upsert declarado en el repositorio;
   sin \texttt{EntityManager}.
\\
\texttt{InstitutionVaccineRepository}
 & \texttt{setEnabled(id, version, enabled)} muerto (13-15).
 & \texttt{setEnabledById} + \texttt{insertEnabledIfAbsent} (nativo
   copy-once).
\\
\bottomrule
\end{longtable}

\section{Arquitectura resultante}

\begin{itemize}
  \item \texttt{attentions} $\to$ \texttt{VaccineCatalogPolicy} (puerto).
  \item \texttt{catalog} $\to$ \texttt{CatalogSelectionService} (implementa el
        puerto; es la única dependencia entre módulos, fuera de \texttt{shared}).
  \item \texttt{shared.application} $\to$ \texttt{IdempotencyCoordinator}.
  \item \texttt{shared.security} $\to$ \texttt{PermissionGuard}.
  \item \texttt{shared.util} $\to$ \texttt{Strings}.
  \item Reglas ArchUnit nuevas en
        \texttt{shared/architecture/ArchitectureTest.java}: frontera
        \texttt{attentions} {$\nrightarrow$} \texttt{catalog.repository/entity}
        y radicación del puerto en \texttt{catalog}.
\end{itemize}

\section{Verificación}

\begin{lstlisting}
mvnw.cmd compile && mvnw.cmd test
\end{lstlisting}

\begin{itemize}
  \item Sin base de datos: todos los tests son JUnit 5 + Mockito puros.
  \item Línea base: 114 tests, 0 fallos.
  \item Tras el refactor: \textbf{116 tests, 0 fallos} (se añaden 2 reglas
        ArchUnit). \texttt{BUILD SUCCESS}.
  \item Tras integrar el PR~\#1 (motor offline): \textbf{127 tests, 0 fallos}.
        Se mantienen las 9 reglas ArchUnit en verde y se suman los tests del
        módulo \texttt{synchronization} (push/pull) y los de pacientes fase 2.
  \item Tras la auditoría SOLID del catálogo: \textbf{140 tests, 0 fallos}
        (\texttt{CatalogSelectionServiceTest} 6, \texttt{CatalogMapperTest} 5,
        \texttt{ReferenceCatalogServiceTest} 2 nuevos). Ver
        sección~\ref{sec:auditoria-catalogo}.
  \item Tras la auditoría SOLID del módulo \texttt{identity}:
        \textbf{146 tests, 0 fallos} (\texttt{IdentityMapperTest} 3 y
        \texttt{IdentityRulesTest} 3 nuevos). Ver
        sección~\ref{sec:auditoria-identity}.
  \item Smoke boot con Supabase tras la integración: el backend arranca
        (\texttt{Started ApiApplication}), Flyway validó 16 migraciones
        (schema V11--V15 en v15) y el health responde \texttt{UP}; los
        endpoints del catálogo y de identity quedan mapeados (401 sin JWT, no
        404); el backend se cierra al terminar la verificación.
  \item Tests actualizados: \texttt{AttentionServiceTest},
        \texttt{PatientServiceTest}, \texttt{InstitutionVaccineServiceCloneTest}
        (ya no mockean \texttt{EntityManager} ni repositorios de catálogo;
        mockean los nuevos colaboradores) y \texttt{UserServiceTest},
        \texttt{UserProvisioningServiceTest}, \texttt{UserMirrorWriterTest}
        (constructores con \texttt{IdentityMapper}/\texttt{PermissionGuard}).
  \item El backend también compila y arranca con \texttt{spring-boot:run}
        (health \texttt{UP}) usando el JDK real
        \texttt{C:\textbackslash Program Files\textbackslash Java\textbackslash jdk-26.0.2.1}.
\end{itemize}

\section{Integración del PR~\#1 (motor offline)} \label{sec:integracion}

El integrante Jorman montó el motor offline (módulo \texttt{synchronization},
pacientes fase 2, catálogos de referencia, migraciones V11--V15 y cliente
Flutter offline) sobre \texttt{master} y lo fusionó vía PR~\#1. La integración
en \texttt{features-brayan} conservó el refactor SOLID y adaptó lo necesario:

\begin{itemize}
  \item \texttt{IdempotencyCoordinator} se adaptó a la firma v2 de
        \texttt{ProcessedOperationsService.record} (6 argumentos con
        \texttt{institutionId} y \texttt{payload}) sin cambiar el contrato
        para los servicios; el payload del comando de dosis conserva
        \texttt{attentionId} para que \texttt{/sync/pull} reconstruya la
        operación.
  \item \texttt{AttentionService}/\texttt{PatientService} mantienen el patrón
        SOLID (coordinator + mapper + \texttt{VaccineCatalogPolicy}) y suman
        overloads offline-first que respetan el \texttt{aggregate\_id} del
        cliente.
  \item \texttt{PatientMapper}/\texttt{AttentionMapper} se ampliaron al perfil
        fase 2 (afiliación, condiciones especiales, condición de usuario).
  \item El módulo \texttt{synchronization} se integró sin reescribirse.
  \item Las reglas ArchUnit no cambiaron y quedaron en verde tras el merge.
\end{itemize}

El entregable de control de cambios (tabla CC-01..CC-11), el checklist de
funcionalidades MVP preservadas y la declaración de uso de IA se incluyen como
apartados de este documento (secciones~\ref{sec:control-cambios},
~\ref{sec:mvp} y~\ref{sec:ia}); la versión reducida vive en
\texttt{docs/engineering/control-de-cambios-solid.md}.

\section{Auditoría SOLID del catálogo} \label{sec:auditoria-catalogo}

Segunda pasada sobre el módulo \texttt{catalog} (estable y completo): se
encontraron dos focos de duplicación y se corrigieron sin cambiar el contrato:

\begin{itemize}
  \item \textbf{DRY/SRP -- \texttt{CatalogMapper} (CC-07)}: el mapeo
        entidad\texttt{-}$>$DTO de \texttt{VaccineResponse}/\texttt{OptionResponse}
        estaba duplicado entre \texttt{VaccineService} (métodos privados
        \texttt{v/o/t}) e \texttt{InstitutionVaccineService}
        (\texttt{toResponse}, \texttt{toVaccineResponse}) más la sugerencia
        inline. El nuevo \texttt{catalog/service/CatalogMapper.java} centraliza
        \texttt{toVaccine}, \texttt{toGlobalOption}, \texttt{toTemplate},
        \texttt{toInstitutionOption}, \texttt{toSuggestedOption} y
        \texttt{toInstitutionVaccine}; ambos servicios orquestan y delegan la
        conversión.
  \item \textbf{DRY -- \texttt{CatalogRules} (CC-08)}: las validaciones de tipos
        de opción (\texttt{validateGlobalType}, \texttt{validateTemplateType},
        \texttt{LOCAL\_OPTION\_TYPES}) y \texttt{requireValue} se movieron a
        \texttt{catalog/service/CatalogRules.java}.
  \item \texttt{GeoCatalogService} y \texttt{ReferenceCatalogService} (solo
        lectura) se auditaron y quedaron sin hallazgos.
\end{itemize}

Verificación: \textbf{140 tests, 0 fallos} sin base de datos; smoke boot con
Supabase (Flyway v15, health \texttt{UP}, endpoints del catálogo mapeados,
\texttt{GeoCatalogImporter} sembrado: 33 departamentos, 1122 municipios).

\section{Auditoría SOLID del módulo identity} \label{sec:auditoria-identity}

Tercera pasada sobre \texttt{identity} (el último módulo estable y habilitado
pendiente). La segunda barrera de permisos re-implementaba manualmente la
ceremonia que \texttt{PermissionGuard} ya centraliza, y el mapeo
entidad{}${}\to{}$DTO se duplicaba:

\begin{itemize}
  \item \textbf{DRY/SRP -- \texttt{IdentityMapper} (CC-09)}:
        \texttt{UserResponse} se construía en cuatro lugares
        (\texttt{UserService.toResponse}, el inline de \texttt{updateStatus} con
        drift de estado, \texttt{UserProvisioningService.replayResult} y
        \texttt{UserMirrorWriter}). El nuevo
        \texttt{identity/service/IdentityMapper.java} centraliza
        \texttt{toUserResponse} (entity; entity + estado explícito para
        actualizaciones scopeadas; operation de aprovisionamiento).
  \item \textbf{DRY/SRP -- \texttt{PermissionGuard} (CC-10)}:
        \texttt{UserProvisioningService} resolvía actor y validaba permiso a
        mano en dos métodos; ahora inyecta \texttt{PermissionGuard}.
        \texttt{IdentityPermissions} centraliza los nombres
        (\texttt{INSTITUTION\_WRITE}, \texttt{USER\_MANAGE}) y
        \texttt{AdminController} usa constantes SpEL. La máquina de estados del
        aprovisionamiento no se tocó.
  \item \textbf{DRY -- \texttt{IdentityRules} y helpers compartidos (CC-11)}:
        \texttt{parseStatus} duplicado quedó en
        \texttt{identity/service/IdentityRules.java}; los privados
        \texttt{trimToNull} y \texttt{normalizeProfessionCode} se reemplazaron
        por \texttt{Strings} y \texttt{DocumentNormalizer} (shared).
\end{itemize}

Evidencia del drift de estado que motivó el \texttt{IdentityMapper} (código real
antes/después):

\begin{lstlisting}[caption={UserService.java (antes) --- mapeo inline con drift en updateStatus}]
private UserResponse toResponse(UserEntity user) {
    return new UserResponse(/* ... */ user.getStatus().name(),
        /* 13 campos mas */);
}

// en updateStatus(scope, operationId, cmd): construye la respuesta CON EL
// ESTADO PARTICIPADO, no el persistido:
return new UserResponse(user.getId(), user.getEmail(), /* ... */,
        parsed.name(), /* drift: parsed == estado solicitado, no el guardado */);
\end{lstlisting}

\begin{lstlisting}[caption={IdentityMapper.java (después) --- una sola fuente de mapeo (fragmento real)}]
@Component
public class IdentityMapper {

    private final UserRepository userRepository;

    public IdentityMapper(UserRepository userRepository) {
        this.userRepository = userRepository;
    }

    /** Estado persistido + roles vigentes (o estado explícito, sin drift). */
    public UserResponse toUserResponse(UserEntity user) {
        return toUserResponse(user, user.getStatus().name());
    }

    public UserResponse toUserResponse(UserEntity user, String status) {
        return new UserResponse(
                user.getId(), user.getEmail(), user.getFullName(),
                user.getInstitutionId(),
                userRepository.findRolesByUserId(user.getId()).stream()
                        .map(RoleEntity::getCode).toList(),
                status, user.getDocumentType(), user.getDocumentNumber(),
                user.getPhone(), user.getBirthDate(), user.getGender(),
                user.getProfessionCode(),
                user.getProfessionalRegistrationNumber(),
                user.getProfessionalRegistrationType());
    }

    /** Replay/mirror de aprovisionamiento: rol unico + ACTIVE. */
    public UserResponse toUserResponse(ProvisioningOperationEntity op) {
        return new UserResponse(
                op.getAuthUserId(), op.getEmail(), op.getFullName(),
                op.getInstitutionId(), List.of(op.getRole()), "ACTIVE",
                op.getDocumentType(), op.getDocumentNumber(), op.getPhone(),
                op.getBirthDate(), op.getGender(), op.getProfessionCode(),
                op.getProfessionalRegistrationNumber(),
                op.getProfessionalRegistrationType());
    }
}
\end{lstlisting}

Evidencia de la ceremonia de permisos centralizada:

\begin{lstlisting}[caption={UserProvisioningService (antes) --- ceremonia manual repetida}]
// en createInstitutionAdmin y createVaccinator (2 metodos):
AuthorizedUser actor = identityService.resolve(actorId);
if (!actor.getPermissions().contains("INSTITUTION_WRITE")) {
    throw new PermissionDeniedException("Permiso insuficiente.");
}
\end{lstlisting}

\begin{lstlisting}[caption={PermissionGuard.java (después) --- guardia unica, SRP}]
@Service
public class PermissionGuard {

    private final IdentityService identity;

    public PermissionGuard(IdentityService identity) { this.identity = identity; }

    /** Resuelve el actor y lanza PermissionDeniedException si falta permiso. */
    public AuthorizedUser require(UUID actorId, String permission) {
        AuthorizedUser actor = identity.resolve(actorId);
        if (!actor.getPermissions().contains(permission)) {
            throw new PermissionDeniedException("Permiso insuficiente: " + permission);
        }
        return actor;
    }
}
\end{lstlisting}

Verificación: \textbf{146 tests, 0 fallos} sin base de datos
(\texttt{IdentityMapperTest} 3 e \texttt{IdentityRulesTest} 3 nuevos); smoke
boot con Supabase (Flyway v15, health \texttt{UP}, \texttt{/api/v1/me} y
\texttt{AdminController} mapeados, 401 sin JWT). Queda sin funcionalidad el
módulo \texttt{reports} (esqueleto) y pendiente una revisión menor de la
infraestructura \texttt{audit} del coordinador.

\section{Control de cambios SOLID (formato evaluación)} \label{sec:control-cambios}

Formato conforme a la guía del taller: cada registro documenta el fallo o
antipatrón detectado, la ubicación exacta, la mejora aplicada y su severidad
(A = Alta, impide escalar el sistema; M = Media, dificulta el mantenimiento;
B = Baja, mejora la legibilidad). Se documentan \textbf{11 registros} (el
mínimo exigido es 5).

El control de cambios documenta la trazabilidad \textbf{fallo $\leftrightarrow$ clase
$\leftrightarrow$ mejora} sobre las clases más representativas de los módulos auditados
(\texttt{attentions}, \texttt{patients}, \texttt{catalog}, \texttt{identity}, \texttt{shared}):
las columnas \textit{Archivo/Clase Modificada} se corresponden con los módulos descritos en
las secciones \ref{sec:antes} y \ref{sec:despues} (clases preexistentes afectadas y clases nuevas
extraídas en la mejora).

\begin{footnotesize}
\begin{longtable}{@{}p{0.9cm}p{3.1cm}p{2.4cm}p{3.5cm}p{3.7cm}p{1.0cm}@{}}
\toprule
\textbf{ID} & \textbf{Archivo / Clase Modificada} &
\textbf{Fallo SOLID / Antipatrón} & \textbf{Descripción del Problema} &
\textbf{Mejora Aplicada} & \textbf{Severidad} \\
\midrule
\endfirsthead
\toprule
\textbf{ID} & \textbf{Archivo / Clase Modificada} &
\textbf{Fallo SOLID / Antipatrón} & \textbf{Descripción del Problema} &
\textbf{Mejora Aplicada} & \textbf{Severidad} \\
\midrule
\endhead
CC-01 & \texttt{patients/service/PatientService.java} & God Object / SRP &
El servicio de $\sim$600 líneas orquestaba la creación, el mapeo entidad{}${}\to{}$DTO, la ceremonia de idempotencia y auditoría, y helpers repetidos (\texttt{blankToNull}, \texttt{parseOperationId}) en un solo lugar, dificultando testing y evolución. &
Se separaron responsabilidades: \texttt{PatientMapper} (mapeo), \texttt{IdempotencyCoordinator} (find $\to$ ejecutar $\to$ auditar $\to$ registrar) y \texttt{Strings} (helpers); el servicio quedó como orquestador del agregado, incluida la fase 2 (afiliación, condiciones, perfil extendido). &
A \\
CC-02 & \texttt{attentions/service/AttentionService.java} & DIP &
El servicio inyectaba repositorios y entidades del módulo \texttt{catalog} (\texttt{VaccineRepository}, \texttt{InstitutionVaccineOptionRepository}, etc.), acoplando módulos de dominio distintos y atando la resolución del catálogo a la implementación concreta. &
Se introdujo \texttt{VaccineCatalogPolicy} como puerto en el dominio de attenciones (resuelto por una implementación en \texttt{catalog}); el servicio solo declara la política. Una regla ArchUnit impide importar \texttt{catalog.entity}/\texttt{catalog.repository} desde \texttt{attentions}. &
A \\
CC-03 & \texttt{shared/application/IdempotencyCoordinator.java} & OCP / DRY &
La ceremonia transversal de la idempotencia (D3) y el parseo de \texttt{operationId} estaban duplicados en \texttt{PatientService} y \texttt{AttentionService}; agregar una operación nueva implicaba replicarla. &
Se extrajo el coordinador transversal: los servicios aportan solo el cuerpo (\texttt{Supplier\textless WriteResult\textless T\textgreater\textgreater}), el contexto de auditoría y el payload; el coordinador ejecuta, audita y registra. &
M \\
CC-04 & \texttt{catalog/repository/InstitutionVaccineRepository.java} & SRP &
El upsert ``copy-once'' para habilitar una vacuna en la institución se ejecutaba desde \texttt{InstitutionVaccineService} con SQL nativo vía \texttt{EntityManager}, dejando la persistencia fuera del repositorio. &
Se movió la query al repositorio con \texttt{@Modifying}/\texttt{@Query} (\texttt{setEnabledById}, \texttt{insertEnabledIfAbsent} nativo con \texttt{ON CONFLICT DO NOTHING}); el servicio solo orquesta. &
M \\
CC-05 & \texttt{patients/service/PatientMapper.java}, \texttt{attentions/service/AttentionMapper.java} & SRP &
El mapeo entidad{}${}\to{}$DTO (incluidos los snapshots de catálogo y el perfil fase 2) vivía dentro de los servicios de negocio. &
Mappers dedicados (\texttt{@Component}) producen \texttt{PatientResponse}/\texttt{AttentionResponse}/\texttt{AppliedDoseResponse}; el dominio delega la conversión y no mezcla persistencia con representación. &
M \\
CC-06 & \texttt{IdempotencyCoordinator} + \texttt{synchronization/service/ProcessedOperationsService.java} & OCP ante cambio de contrato &
El PR~\#1 de Jorman evolucionó el contrato de \texttt{ProcessedOperationsService.record} (firma de 6 argumentos con \texttt{institutionId} y \texttt{payload}) e introdujo el motor offline (\texttt{/sync/push}, \texttt{/sync/pull}, cola Flutter). El coordinador previo (4 args) quedaba incompatible. &
El coordinador se adaptó a la firma 6-arg manteniendo estable el contrato para los servicios de negocio (el payload viaja como \texttt{Supplier\textless Object\textgreater}; en dosis incluye \texttt{attentionId} para que \texttt{/sync/pull} reconstruya la operación). El módulo \texttt{synchronization} y el motor offline se integraron sin reescribirlos. &
A \\
CC-07 & \texttt{catalog/service/VaccineService.java}, \texttt{catalog/service/InstitutionVaccineService.java} & DRY / SRP &
El mapeo entidad{}${}\to{}$DTO del catálogo se construía en tres lugares: \texttt{VaccineService} (\texttt{v()}, \texttt{o()}, \texttt{t()}), \texttt{InstitutionVaccineService} (\texttt{toResponse}, \texttt{toVaccineResponse}, y la sugerencia inline) y el catálogo efectivo; \texttt{VaccineResponse} y \texttt{OptionResponse} se duplicaban campo a campo, arriesgando desalineación al crecer la fase 2. &
Nuevo \texttt{catalog/service/CatalogMapper.java} (\texttt{@Component}) centraliza todo el mapeo (\texttt{toVaccine}, \texttt{toGlobalOption}, \texttt{toTemplate}, \texttt{toInstitutionOption}, \texttt{toSuggestedOption}, \texttt{toInstitutionVaccine}); ambos servicios orquestan y delegan la conversión. &
M \\
CC-08 & \texttt{catalog/service/VaccineService.java}, \texttt{catalog/service/InstitutionVaccineService.java} & DRY &
Las validaciones de tipos de opción (\texttt{validateGlobalType}, \texttt{validateTemplateType}, \texttt{LOCAL\_OPTION\_TYPES} + \texttt{validateType}) y \texttt{requireValue} estaban duplicadas como privadas en cada servicio; añadir un tipo nuevo exigía tocar dos clases. &
Nuevo \texttt{catalog/service/CatalogRules.java} con las reglas compartidas (\texttt{requireGlobalOptionType}, \texttt{requireTemplateType}, \texttt{requireLocalOptionType}, \texttt{requireValue}); los servicios solo invocan. Complementa el \texttt{CatalogMapper} (CC-07). &
B \\
CC-09 & \texttt{identity/service/UserService.java}, \texttt{identity/service/UserProvisioningService.java}, \texttt{identity/service/UserMirrorWriter.java} & DRY / SRP &
El mapeo entidad{}${}\to{}\texttt{UserResponse} (14 campos) se construía en cuatro lugares: \texttt{UserService} (\texttt{toResponse} y el inline de \texttt{updateStatus}, con un drift entre \texttt{parsed.name()} y \texttt{user.getStatus().name()}), \texttt{UserProvisioningService.replayResult} y \texttt{UserMirrorWriter}. identity no tenía componente mapper (a diferencia del catálogo). &
Nuevo \texttt{identity/service/IdentityMapper.java} (\texttt{@Component}) centraliza el mapeo: \texttt{toUserResponse(entity)}, \texttt{toUserResponse(entity, status)} para el estado recién aplicado en actualizaciones scopeadas, y \texttt{toUserResponse(operation)} para el replay/mirror con rol único y \texttt{ACTIVE}. &
M \\
CC-10 & \texttt{identity/service/UserProvisioningService.java} & DRY / SRP &
La ceremonia de permiso manual (\texttt{identityService.resolve} + \texttt{permissions.contains} + \texttt{throw PermissionDeniedException}) se repetía en dos métodos y era el mismo antipatrón que \texttt{PermissionGuard} centraliza para el catálogo; además los literales \texttt{INSTITUTION\_WRITE}/\texttt{USER\_MANAGE} se duplicaban en servicios, \texttt{DataScope} y los \texttt{@PreAuthorize} de \texttt{AdminController} (incluido el SpEL combinado pegado 3 veces). &
Se inyectó \texttt{PermissionGuard} (resuelve actor + valida permiso con mensaje único) y se creó \texttt{identity/service/IdentityPermissions.java} con los nombres canónicos; \texttt{AdminController} usa constantes SpEL (\texttt{WRITE\_ONLY}, \texttt{MANAGE\_ONLY}, \texttt{MANAGE\_OR\_WRITE}) al estilo de \texttt{GeoCatalogController}. &
M \\
CC-11 & \texttt{identity/service/UserService.java}, \texttt{identity/service/InstitutionService.java}, \texttt{identity/service/UserProvisioningService.java} & DRY &
\texttt{parseStatus} estaba duplicado entre \texttt{UserService} e \texttt{InstitutionService}; \texttt{UserProvisioningService} re-implementaba \texttt{trimToNull} ($\equiv$\texttt{Strings.trimToNull}) y \texttt{normalizeProfessionCode} ($\equiv$\texttt{DocumentNormalizer.normalizeType}) sin usar los helpers compartidos. &
Nuevo \texttt{identity/service/IdentityRules.java} (util final) con \texttt{parseUserStatus}/\texttt{parseInstitutionStatus}; los helpers privados se reemplazaron por \texttt{Strings}/\texttt{DocumentNormalizer}. &
B \\
\bottomrule
\end{longtable}
\end{footnotesize}

\section{Checklist de funcionalidades MVP preservadas} \label{sec:mvp}

Verificación funcional tras el refactor y las integraciones (todas en
\textbf{146 tests, 0 fallos} y smoke boot contra Supabase):

\begin{itemize}
  \item[\checkmark] Registro de pacientes con sus cinco subentidades (afiliación,
        condiciones especiales, condición de usuario, contactos, historial).
  \item[\checkmark] Creación de atenciones y \texttt{registerDose} con
        idempotencia por \texttt{operationId}.
  \item[\checkmark] Cancelación verificada (drift D3) de atenciones y dosis con
        auditoría específica \texttt{Cancellation}.
  \item[\checkmark] Actualizaciones parciales de paciente (contacto,
        demográficos, historial médico) con auditoría.
  \item[\checkmark] Catálogo: vacunas, opciones globales, plantillas, opciones
        institucionales y habilitación \texttt{copy-once} por institución.
  \item[\checkmark] Aprovisionamiento de usuarios (\texttt{createInstitutionAdmin},
        \texttt{createVaccinator}) con SDK y máquina de estados.
  \item[\checkmark] \texttt{/api/v1/me}, login/refresh y \texttt{AdminController}
        con permisos \texttt{INSTITUTION\_WRITE}/\texttt{USER\_MANAGE}.
  \item[\checkmark] Sincronización offline-first (\texttt{/sync/push},
        \texttt{/sync/pull}) con agregado del cliente respetado.
  \item[\checkmark] Fronteras arquitectónicas en verde (9 reglas ArchUnit:
        \texttt{attentions} no depende de \texttt{catalog.repository/entity};
        el puerto se implementa en \texttt{catalog}).
\end{itemize}

\section{Diagrama de Clases UML actualizado} \label{sec:diagrama}

El siguiente diagrama de clases refleja la estructura \emph{resultante} tras
el refactor (frontera \texttt{attentions}, puertos, mappers, coordinador y
reglas de permisos). Fue generado con PlantUML desde
\texttt{docs/engineering/diagrama-clases-solid.puml}:

\begin{landscape}
\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{diagrama-clases-solid.png}
  \caption{Diagrama de clases UML del backend refactorizado \texttt{services/api}.}
\end{figure}
\end{landscape}

\begin{table}[p]
\centering
\caption{Comparación entre la entrega 1 y la entrega actual}
\label{tab:compara-uml}
\begin{tabular}{@{}p{4.6cm}p{5.2cm}p{5.2cm}@{}}
\toprule
\textbf{Aspecto} & \textbf{Entrega 1} & \textbf{Entregable actual} \\
\midrule
Arquitectura & Servicios tipo \emph{god class} con mapeo,
persistencia y validación dentro del controlador/servicio. &
Fronteras claras: \texttt{patients}/\texttt{attentions} dependen del puerto
\texttt{VaccineCatalogPolicy}; el catálogo lo implementa. \\
Inversión de dependencias & Sin puertos; el módulo clínico inyectaba repositorios
JPA del catálogo. &
Dependencia invertida vía puerto; regla ArchUnit impide la dependencia directa. \\
Mapeo & \texttt{toResponse} privado duplicado en cada servicio. &
Mappers dedicados por módulo (\texttt{PatientMapper}, \texttt{AttentionMapper},
\texttt{CatalogMapper}, \texttt{IdentityMapper}). \\
Transversal & Ceremonia idempotente repetida en cada operación. &
\texttt{IdempotencyCoordinator} único (\texttt{shared.application}). \\
Permisos & \texttt{actor(actorId, permission)} duplicado en cada servicio. &
\texttt{PermissionGuard} único (\texttt{shared.security}) + constantes
(\texttt{IdentityPermissions}). \\
Reglas de dominio & Validaciones embebidas en los servicios. &
\texttt{CatalogRules} e \texttt{IdentityRules} (utils finales SRP). \\
\bottomrule
\end{tabular}
\end{table}

\section{Declaración de uso de Inteligencia Artificial} \label{sec:ia}

Conforme a la guía del taller, se declara el uso de herramientas de IA en la
elaboración de esta entrega: el diagnóstico de fallos SOLID/antipatrones
(CC-01..CC-11), la propuesta de refactorización y la redacción de este
documento fueron elaborados con el auxilio de dichas herramientas.
\textbf{Todo el código fue revisado, aplicado y verificado por el equipo}:
cada cambio compila (\texttt{mvnw compile}), supera las \textbf{146 pruebas
unitarias sin fallos}, se validó con arranque real contra Supabase (Flyway v15,
health \texttt{UP}) y se inspeccionó manualmente. El equipo comprende y está en
capacidad de sustentar cada cambio documentado; los fragmentos de código
incluidos en este informe corresponden a clases reales del repositorio.

\section{Recomendaciones futuras}

\begin{enumerate}
  \item \textbf{Auditar la infraestructura \texttt{audit}} del coordinador con
        las reglas ArchUnit (revisión menor; no se audita hasta ahora) y
        acompañar el futuro \texttt{reports} con el mismo estándar.
  \item \textbf{Cancelaciones en el coordinador}: añadir un overload de
        \texttt{executeAudited} con \texttt{payloadFn} para llevar
        \texttt{cancel}/\texttt{cancelDose} a la misma ceremonia sin perder su
        payload de auditoría compuesto.
  \item \textbf{Evitar la doble resolución del actor} en cuerpos que ya lo
        resuelven: evaluar que \texttt{AuditContext} transporte el
        \texttt{AuthorizedUser} en lugar de solo el id.
  \item \textbf{Dependencia residual}: \texttt{AttentionService} aún importa
        \texttt{catalog.exception.OptimisticCatalogException}; migrar esa
        excepción genérica de versión optimista a \texttt{shared.exceptions}.
  \item \textbf{Tests}: falta cubrir \texttt{PermissionGuard} con un test
        directo (hoy se ejercita vía los servicios que lo inyectan).
  \item \textbf{MapStruct}: si el volumen de mapeo crece, evaluar MapStruct en
        lugar de mappers manuales (sin cambiar la estructura expuesta).
  \item \textbf{Sync}: el \texttt{SyncEngine} (PR~\#1) ya se integró; el
        \texttt{/sync/push} comparte la garantía D3 vía
        \texttt{IdempotencyCoordinator} (adaptado a la firma v2). Los catálogos
        geográficos e \texttt{identity} ya fueron auditados (secciones
        ~\ref{sec:auditoria-catalogo} y~\ref{sec:auditoria-identity});
        \texttt{reports} se auditará cuando exista funcionalidad.
\end{enumerate}

\end{document}